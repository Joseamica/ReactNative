import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const LiveSessionScreen = () => {
  return (
    <View>
      <Text>life sessions</Text>
    </View>
  );
};

export default LiveSessionScreen;

const styles = StyleSheet.create({});
